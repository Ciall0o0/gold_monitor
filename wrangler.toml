name = "gold-monitor"
main = "src/index.ts"
compatibility_date = "2026-01-01"

# Cron trigger - every 15 minutes during trading hours
[triggers]
crons = ["0 0/15 * * * 1-5"]

# Environment variables (set via wrangler secret or dashboard)
[vars]
# OPENROUTER Configuration
OPENROUTER_MODEL = "nvidia/nemotron-3-super-120b-a12b:free"
OPENROUTER_API_URL = "https://openrouter.ai/api/v1/chat/completions"

# Alert Thresholds
MA_WINDOW = "5"
PRICE_DEVIATION_THRESHOLD = "0.05"
AI_CONFIDENCE_THRESHOLD = "70"

# Price Data Sources
PRICE_API_URL = "https://hq.sinajs.cn/list=hf_AU0"

# News Data Source
NEWS_API_URL = "https://orz.ai/api/v1/dailynews/?platform=cls"

# KV Namespace for caching price history
[[kv_namespaces]]
binding = "PRICE_HISTORY"
id = "8a0b17db368a44a6977a96418da47f70"

# Secrets (use `wrangler secret put` to set these):
# - OPENROUTER_API_KEY
# - SERVERCHAN_SENDKEY
# - JINA_API_KEY

[observability]
[observability.logs]
enabled = true
invocation_logs = true